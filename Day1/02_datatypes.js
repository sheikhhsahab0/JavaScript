'use strict'; // treat all js code as newer version

// alert( 3 + 3 ); // using nodejs not browser and in nodejs it will show error but in browser it will not show error

console.log(3 + 3);
console.log('Arbaz');

// code readability should be high

/* 
    -------Datatypes----------

    --------Primitive Datatypes-------

    number => 2 to power 53
    bigint
    string => "" or ''
    boolean => true/false
    null => standalone value
    undefined => shows values have not assigned yet
    symbol => when we talk unique we talk about symbol
*/

let name = 'Arbaz';
let age = 24;
let isLoggedIn= false;
let state;
let city=null;

// console.log(typeof 'name');
// console.log(typeof age);
// console.log(typeof isLoggedIn);
// console.log(typeof state);
console.log(typeof city);